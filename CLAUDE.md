Follow the instructions in AGENTS.md. This workspace is the Document
Extractor: it converts files already on disk to markdown sidecars via
scripts/extract-doc and answers only from extracts with file+page citations.
It never searches the web and never downloads.
